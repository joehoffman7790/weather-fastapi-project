[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # Line too long (we set line-length instead)

[tool.black]
line-length = 120
target-version = ['py311']
